/*
  水仙花數:用來描述一個N位非負整數，其各位數字的N次方和等於該數本身。 
  例如：13+53+33=153，14+64+34+44=1634，55+45+75+45+85=54748。
  題目：求所有三位數中的水仙花數
*/
#include<stdio.h>
#include<math.h>

int main(){
	int i;	
	for(i=101;i<1000;i++){
		int a = i % 10;
		int b = (i/10) % 10;
		int c = (i/100) % 10;
		int d = pow(a, 3)+pow(b, 3)+pow(c, 3);
		
		if(d == i){
			printf("%d\n", i);
		}
	}
}

//延伸:求出所有輸入之Ｎ位數的水仙花數
//判斷位數介於10^n~10^(n+1)，使用for迴圈檢測該數是否為水仙花數
